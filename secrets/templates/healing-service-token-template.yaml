apiVersion: v1
kind: Secret
metadata:
  name: healing-service-secrets
  namespace: autoops-ops
type: Opaque
data:
  # Replace each value with: echo -n 'actual-value' | base64
  anthropic-api-key: PLACEHOLDER  # kept for reference — actual key is OPENROUTER_API_KEY
  openrouter-api-key: PLACEHOLDER
  argocd-token: PLACEHOLDER
  webhook-secret: PLACEHOLDER
