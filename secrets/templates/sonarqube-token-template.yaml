apiVersion: v1
kind: Secret
metadata:
  name: sonarqube-token
  namespace: autoops-ops
type: Opaque
data:
  # Replace with: echo -n 'your-sonar-token' | base64
  token: PLACEHOLDER_BASE64_SONAR_TOKEN
